{
  "name": "receipts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Receipts \u2014 a Slack agent that ends arguments with the actual prior decision (RTS search-as-you \u2192 verbatim quote + permalink).",
  "scripts": {
    "spike": "bun run scripts/spike.ts",
    "seed": "bun run scripts/seed.ts",
    "dev": "bun run src/app.ts",
    "stage": "bun run scripts/stage.ts"
  },
  "dependencies": {
    "@slack/bolt": "^4.2.0",
    "@anthropic-ai/sdk": "^0.68.0",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
